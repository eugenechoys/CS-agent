# Employee CS Source Summary Guidance

Use a short evidence-backed explanation when live data is used:

- Mention the selected tenant and, if relevant, the selected employee.
- Mention only the key fields that informed the answer.
- Do not dump raw JSON.
- If the data is mixed from live and demo fallback sources, say that succinctly.
